# Development

This section documents how contributors set up, run, test, format, and debug the project.

## Phase 0 principle

A new contributor should eventually be able to clone the repository, install the required dependencies, run validation, and start the development environment using documented commands.

The exact toolchain will be recorded here once the initial client/server stack is selected.
